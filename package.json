{
  "name": "tidy-master",
  "version": "1.0.0",
  "description": "TidyMaster - 微信小游戏收纳整理游戏",
  "scripts": {
    "build": "tsc --noEmit",
    "type-check": "tsc --noEmit",
    "verify:chapter1-drag-loop": "node -e \"console.log('正式验证入口源码位于 scripts/verification/verify-chapter1-drag-loop.ts；如需执行请在本地按项目编译链路运行。')\"",
    "archive:verify:merge-loop": "node -e \"console.log('archive/internal 占位脚本：历史 merge loop 资产已归档至 archive/history/scripts-tmp/verify-merge-loop')\""
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/node": "^20.0.0"
  }
}
